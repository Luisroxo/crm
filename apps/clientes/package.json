{
  "name": "clientes",
  "version": "0.1.0",
  "description": "Microserviço de Clientes do CRM",
  "main": "dist/main.js",
  "scripts": {
  "prestart": "pnpm exec prisma generate --schema=prisma/schema.prisma",
  "prestart:dev": "pnpm exec prisma generate --schema=prisma/schema.prisma",
  "prebuild": "pnpm exec prisma generate --schema=prisma/schema.prisma",
  "start": "nest start",
  "start:dev": "nest start --watch",
    "build": "nest build"
  },
  "dependencies": {
    "@prisma/client": "6.16.2",
    "class-validator": "^0.14.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "@nestjs/mapped-types": "^2.0.0"
  },
  "devDependencies": {
    "prisma": "6.16.2",
    "typescript": "^5.0.0",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.2",
    "@types/jest": "^30.0.0",
    "supertest": "^6.3.4",
    "@types/node": "^20.3.1",
      "@nestjs/cli": "^10.0.0",
      "@nestjs/testing": "^10.0.0"
  }
}
