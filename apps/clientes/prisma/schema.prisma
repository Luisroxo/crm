generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-clientes"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_CLIENTES")
}

// Adicione seus models aqui
