generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-tarefas"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_TAREFAS")
}

// Adicione seus models aqui
