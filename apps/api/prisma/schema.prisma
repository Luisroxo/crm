generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-api"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_API")
}

// Adicione seus models aqui
