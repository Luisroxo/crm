generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-empresas"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_EMPRESAS")
}

// Adicione seus models aqui
