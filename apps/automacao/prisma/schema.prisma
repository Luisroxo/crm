generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-automacao"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_AUTOMACAO")
}

// Adicione seus models aqui
