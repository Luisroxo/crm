generator client {
  provider = "prisma-client-js"
  output   = "../../node_modules/.prisma/client-vendas"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_VENDAS")
}

// Adicione seus models aqui
